# 更新日志

[English](CHANGELOG.md)

按版本记录主要更新，最新版本列在最前。

## 1.6.17 - 2026-09-01

### 调整

- 统一优化界面与文档中的隐私和数据保护说明。

### 验证

- 界面、本地化相关的精简测试及静态检查均已通过。

## 1.6.16 - 2026-08-29

### 调整

- README 开头新增项目背景，说明重复经过 Okta 与 Duo 登录步骤带来的不便，以及本插件如何简化这一流程。
- 精简「本地数据」说明，仅保留卸载插件会删除其所保存数据的提示。

### 验证

- README 双语结构、版本信息、本地化和 Manifest 相关的静态检查均已通过。

## 1.6.15 - 2026-08-29

### 调整

- 更新插件简介，突出更简单、更快捷的 UChicago 账号登录体验。

### 验证

- Manifest、版本信息、本地化和文档相关的静态检查均已通过。

## 1.6.13 - 2026-08-29

### 调整

- 验证 PIN 设置现在会显示简明的用途说明；展开该栏目后，才会根据保存状态加载相应的加密说明。

### 验证

- 设置界面、本地化和主题相关的 62 项精简测试全部通过。

## 1.6.12 - 2026-08-29

### 调整

- 「账号」栏目中的加密说明现在会明确说明用户名和密码均会安全保存。

## 1.6.11 - 2026-08-29

### 调整

- 设置页会根据密码和通行密钥的保存状态，说明相应的本地加密方式与授权使用范围。

### 验证

- 设置界面、本地化和主题相关的 62 项精简测试全部通过。

## 1.6.10 - 2026-08-29

### 新增

- UChicago 账号登录确认窗口现在会显示简短的登录地址说明。

### 调整

- 插件界面及文档中的第二人称称谓统一使用「您」。

### 验证

- 登录确认与本地化相关的精简测试及静态检查均已通过。

## 1.6.9 - 2026-08-29

### 调整

- 在 my.UChicago 门户确认登录后，会立即进入固定的 AIS 学生登录地址，不再等待门户页面或导航控件加载。
- Okta 的账号字段和具有固定结构的提交按钮进入当前页面后即可处理；隐藏模板、密码找回、验证码、错误状态及有歧义的控件仍会排除。
- 增补 Duo 中文页面的兼容，并通过验证方式列表和“管理设备”控件的页面结构进行识别，减少对界面文案的依赖。

### 修复

- 设置页中，通行密钥对应的账号与“当前账号”标记现在会在同一水平线上显示。

### 验证

- 界面、本地化、流程控制、通行密钥适配和网页操作相关的 213 项精简测试全部通过。

## 1.6.8 - 2026-08-29

### 新增

- 保存账号后，可按引导添加 Duo 通行密钥。插件会重新开始学校账号登录，进入设备管理，在需要时提示完成身份验证，确认注册结果后返回登录。
- 支持从 HTTP 或 HTTPS 的 my.UChicago 首页直接确认登录，并兼容门户、Courses、Okta 应用入口及使用 UChicago 账号的第三方应用。
- Duo 验证后如出现“这是您的设备吗？”页面，会自动选择当前设备。

### 调整

- 当前账号只要有可用的通行密钥，就会保持自动验证；没有可用密钥时，Duo 验证由用户手动完成。
- 重新设计「Duo 与通行密钥」，直接显示当前验证方式、密钥对应账号及失效状态，并保留由用户手动删除密钥的操作。已有可用密钥时，不再显示添加入口。
- 优化 Okta 与 Duo 页面操作，兼容登录跳转、延迟出现的控件、不同 Duo 子域名，以及英文和简体中文界面。
- 最近活动仅保留过去 24 小时内的最多 20 条；首次安装后自动打开设置页。

### 修复

- 在 Chrome 弹出通行密钥窗口前接管支持的 Duo Security Key 请求；Duo 预先打开其他常用验证方式时，也会先切换到安全密钥。
- Duo 明确拒绝的通行密钥会保留在本地并标记为「已失效」，可按引导重新添加，不会自动删除原有密钥。
- 修复已选择简体中文时，页面打开后短暂显示英文的问题。

### 验证

- 界面、本地化、流程控制、通行密钥适配和网页操作相关的 200 项精简测试，以及静态检查均已通过。

## 0.2.11 - 2026-08-28

### 调整

- 优化首次打开时的语言选择，并保留手动设置。
- 调整设置页名称的字号。

### 修复

- 修复设置页、控制窗口和确认窗口打开时先闪现英文、再切换至已选语言的问题。
- 语言加载完成前，标签页标题仅显示 UChicago；读取语言失败时，页面仍可正常使用。

### 验证

- 界面、本地化和主题相关的 48 项精简测试，以及选取的数据清除回归测试均已通过，涵盖默认语言、手动选择、读取延迟、读取失败及跨窗口切换。

## 0.2.10 - 2026-08-28

### 调整

- 重写中英文 README，面向仓库访问者介绍安装、日常使用、通行密钥配置、隐私、权限和常见问题。
- 重新梳理设置、确认窗口、操作按钮、活动记录和帮助说明的中文文案。
- 调整设置页名称的字号。
- 更新设置页「开始使用」，说明学校及第三方服务登录、Duo 自动处理和通行密钥注册步骤。
- 统一设置页底部版本号与上下两行之间的间距。

### 验证

- 界面、本地化和主题相关的 43 项精简测试全部通过。

## 0.2.9 - 2026-08-28

### 调整

- 移除 Duo 网址配置和逐站点状态显示，改为依据已确认的学校登录流程授权，支持不同 Duo 子域名和 HTTPS 中间跳转。
- 支持从第三方应用发起登录及返回第三方网站，也支持不经过 Duo 就直接返回应用的流程。
- 提前准备 Duo 适配器，短暂等待浏览器确认跳转；不属于已授权流程的请求交还浏览器处理。
- 活动记录仅保留过去 24 小时内的最多 20 条，过期内容自动从本地删除，设置页同步更新。
- 调整设置页名称的字号。

### 验证

- 流程、适配器、界面、本地化、策略和主题相关的精简检查通过。

## 0.2.8 - 2026-08-28

### 调整

- 将设置页顶部的 logo 从 32px 放大到 64px，与右侧名称的间距调整为 14px。
- 同步更新中英文 README 和图标说明中的显示尺寸。
- 移除登录确认窗口中的“For this tab only.”提示行及对应中文文案。

### 验证

- 9 项主题精简检查及静态检查通过。

## 0.2.7 - 2026-08-28

### 调整

- 中文 README 改为当前版本的完整使用指南，按中文阅读习惯重新撰写，内容与英文版对应，按钮和栏目名称与中文界面一致。
- 完善两份 README 中的配置步骤、权限、隐私、快捷键和开发说明。
- 增加轻量检查，核对两份 README 的章节、表格、代码中的固定值、命令及链接地址。
- 在中英文 README 末尾新增独立项目声明，明确说明无隶属、赞助或背书关系。

### 验证

- 静态检查和文档专项检查通过。

## 0.2.6 - 2026-08-28

### 新增

- 在现有确认窗口中加入 Enter / 空格键确认、Esc 取消，并为辅助技术提供按钮快捷键信息。
- 在折叠的“详情”现有说明之后另起一行，添加随全局语言切换的快捷键提示。

### 行为

- 输入框、通行密钥选择、其他按钮及“详情”控件保留原有的 Enter / 空格键操作。确认快捷键忽略输入法组合输入、修饰键组合、合成事件和长按重复。
- 快捷键沿用现有决策流程，保留必填 PIN 校验、禁用状态、请求有效期及防止重复提交的保护。请求不可用或过期时，仍可用 Esc 关闭窗口。

### 验证

- 两个测试组中的 33 项界面与本地化精简测试全部通过。

## 0.2.5 - 2026-08-28

### 调整

- 设置页、控制窗口和确认窗口的中文默认字距统一为 `0.04em`，英文统一为 `normal`，涵盖标题和表单控件。
- 将账号和通行密钥状态图标统一为与顶部操作图标相同的 18px 宽度；横向账号卡片等比缩至约 13.8px 高，不再占用突兀的大尺寸。
- 移除标题的负字距。字距随所选语言即时切换，并根据各元素自身字号计算。
- 为英文品牌文字和语言选项分别标注语言，确保它们在任一界面语言下使用正确字距。
- 说明 Chrome 原生对话框和原生悬停提示的排版由浏览器控制，插件无法通过 CSS 修改。

### 验证

- 排版、主题和本地化相关的 24 项精简测试通过。

## 0.2.4 - 2026-08-28

### 调整

- 将账号已保存图标重绘为 26×20 像素的横向证件卡，放大圆形头像，采用开放的肩部轮廓，并拉开账号和密码信息的间距。
- 保留各状态图标原始的 SVG 比例并禁止弹性压缩，不再统一为相同的正方形尺寸。
- 将两个设置保存按钮统一为“保存”，帮助文案按栏目区分，英文同步使用 **Save**。
- 将中英文页脚中的表述顺序调整为先“赞助”、后“背书”。
- 电源图标的悬停提示同时说明当前启用状态和点击后的动作；仅这两句英文采用精简表达。

### 新增

- 在设置页帮助栏新增默认折叠的双语“隐私说明”。

### 验证

- 界面、本地化和主题相关的 35 项精简测试通过，包含状态图标比例的回归检查。
- 检查了账号卡片在明暗背景、原尺寸和 4 倍尺寸下的独立 SVG 预览，并依据本地实现核对隐私说明。

## 0.2.3 - 2026-08-28

### 调整

- 将控制窗口宽度从 320px 缩至 280px，收紧上下留白，品牌名称改为单行。顶部 28px 的电源、重试登录和设置按钮保留键盘操作，窗口高度随内容变化。
- 分别用证件卡和钥匙图标表示已保存的账号密码及本地通行密钥。仅在对应数据存在时显示，不依赖是否选择了通行密钥或配置了 Duo。
- 仅在配置了 Duo 站点时显示 Duo 状态行。
- 同步扩展中英文声明，将无隶属关系、未获背书及赞助的范围明确涵盖任何其他组织机构。
- 统一优化两种界面语言中的本地数据控件及相关说明。

### 新增

- 新增包含完整版本记录的 `CHANGELOG_zh.md`，两个语言版本互相链接。
- 交付包包含两份更新日志，并检查两者的版本号和日期序列是否一致。

### 验证

- 全部 116 项本地测试和静态检查通过，涵盖独立显示保存状态图标、隐藏未配置的 Duo 状态，以及删除确认和取消。

## 0.2.2 - 2026-08-28

### 调整

- 将控制窗口下方单独的启用开关行改为顶部电源图标，放在重试登录和设置图标左侧。
- 保留即时保存状态、防止重复点击，以及更新失败后的状态恢复。酒红色底表示启用，灰色线条图标表示暂停。
- 保留键盘操作、供辅助技术读取的开关状态和中英文操作提示，不增加可见文字标签。

### 新增

- 将更新日志加入源码和交付包。
- 增加发布检查，要求项目版本、扩展版本和最新日志条目的版本一致。

### 验证

- 全部 114 项本地测试和静态检查通过，涵盖图标顺序、启用状态保存和提示本地化。

## 0.2.1 - 2026-08-28

### 调整

- 恢复两栏设置页，左侧为配置，右侧为帮助；左下方的已保存通行密钥和验证 PIN 保持默认折叠。
- 将“保存账号”和“保存设置”分开。保存其中一栏会保留另一栏的未保存输入；更换账号必须输入密码，且只清除已选择的通行密钥关联。
- 为控制窗口增加即时保存的启用开关，将设置和重试登录改为图标按钮。Duo 状态精简为一行，不显示空的通行密钥数量。
- 将两种系统外观下的交互红色统一为 `#800000`，并保持文字、控件边界和焦点标识清晰可辨。
- 在设置页底部明确声明：本项目与芝加哥大学、Okta 或 Duo Security 无隶属关系，未获其背书或赞助。
- 同步更新中英文界面文案。

### 验证

- 本地测试增至 114 项且全部通过，涵盖独立保存、保留未保存输入、开关状态保存和错误恢复。

## 0.2.0 - 2026-08-28

### 新增

- 访问 `https://portal.uchicago.edu/ais/` 时询问是否登录，确认后使用 Student 栏目的 my.UChicago 登录链接。
- 访问 `https://courses.uchicago.edu/` 时询问是否登录，确认后进入固定的 Canvas `/login/1` 地址。
- 增加对应的 portal 和 Courses 站点权限，同时保留对入口路径的严格限制。

### 调整

- 改善对 Okta `/app/*` 应用登录表单的识别，涵盖延迟加载的用户名、密码和合并表单。
- 在同一标签页中，经固定入口地址延续已批准的登录流程，避免在 Okta 重复确认；保留授权期限和凭据释放限制。
- 精简双语设置页、控制窗口和确认窗口，移除控制窗口底部冗余的存储说明。

### 验证

- 本地测试增至 98 项且全部通过，涵盖入口跳转、延迟加载表单、取消和撤销站点访问权限。

## 0.1.0 - 2026-08-28

### 新增

- 打包首个版本：在识别到的 Okta 登录步骤前请求确认，并在本地保存账号。
- 为指定的 Duo 站点提供本地软件通行密钥功能，支持明确授权和验证 PIN。
- 默认使用美式英语，可切换简体中文；语言设置跨窗口共享，外观自动跟随系统明暗主题。
- 增加酒红色凤凰与钥匙图标，使用透明圆角；明确声明 Okta 和 Duo 站点权限。

### 验证

- 已保存的交付版本包含 72 项通过的本地测试。
